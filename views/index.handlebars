<h1>BURGER!</h1>

<h2>Burgers that were devoured!</h2>

<ul>
  {{#each burger}}
    {{#unless devoured}}
      {{> burger/burger-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that weren't devoured!</h2>

<ul>
  {{#each burger}}
    {{#if devoured}}
      {{> burger/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="food">Burger Name:</label>
    <input type="text" id="food" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured?</label><br>
    <input type="radio" name="status" value="1" checked> Yes!<br>
    <input type="radio" name="status" value="0"> No
  </div>

  <button type="submit">New Addition</button>
</form>